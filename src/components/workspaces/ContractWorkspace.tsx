// =============================================================================
// DMSuite - Contract Workspace (Thin Wrapper)
// Routes to the new Contract Designer Workspace.
// =============================================================================

"use client";

import ContractDesignerWorkspace from "./contract-designer/ContractDesignerWorkspace";

export default function ContractWorkspace() {
  return <ContractDesignerWorkspace />;
}
